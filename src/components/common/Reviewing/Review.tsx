import React from 'react';
import './Review.css'; 
import UserIcon from "../../assets/UserIcon.png"

interface ReviewText{
    text?: string;
}

const ReviewButton: React.FC<ReviewText> = ({text}) => {
    return (
        <div>

        </div>
    );
}

export default ReviewButton;
